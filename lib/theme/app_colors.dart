import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const bg = Color(0xFFF5F3EF);
  static const cardBg = Color(0xFFFFFFFF);
  static const nutritionCardBg = Color(0xFFEEEBE6);

  // Teal / primary
  static const teal = Color(0xFF4ECDB4);
  static const tealLight = Color(0xFFE8F8F5);
  static const tealDark = Color(0xFF3AB8A0);

  // Accent colors
  static const pink = Color(0xFFFF8FAB);
  static const pinkLight = Color(0xFFFFE8EF);
  static const purple = Color(0xFFB8A9E8);
  static const purpleLight = Color(0xFFF0EDFB);
  static const orange = Color(0xFFFF9874);
  static const orangeLight = Color(0xFFFFEDE8);

  // Text
  static const textDark = Color(0xFF1A1A2E);
  static const textMedium = Color(0xFF6B7280);
  static const textLight = Color(0xFF9CA3AF);

  // Divider
  static const divider = Color(0xFFE5E7EB);

  // Header gradient
  static const headerStart = Color(0xFF4ECDB4);
  static const headerEnd = Color(0xFF45C4AB);
}

class AppTextStyles {
  static const String fontFamily = 'Nunito';
}